"""ThreatLens backend package."""

# Embedded verbatim in generated YARA/Chronicle rule content (see
# detection/future/{yara,chronicle}.py), so it is part of the frozen Detection
# Engine v1.0 golden output. Bump only alongside an intentional golden
# regeneration — not for ordinary package/release version bumps (see
# pyproject.toml's `version` for the release/package version instead).
__version__ = "1.0.0"
